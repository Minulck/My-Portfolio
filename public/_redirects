# Netlify redirects file

# SPA fallback (for client-side routing)
/*    /index.html   200

# Optional: Force HTTPS (if not using Netlify's automatic HTTPS)
# http://minul.netlify.app/*    https://minul.netlify.app/:splat    301!

# Optional: Redirect www to non-www (if needed)
# https://www.minul.netlify.app/*    https://minul.netlify.app/:splat    301!
